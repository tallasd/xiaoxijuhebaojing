# 本地邮件中转采集器

这个采集器部署在远程电脑或海外主机上。

浏览器插件把消息发到：

```text
http://127.0.0.1:4222
```

本地采集器再用 SMTP 把结构化邮件发到统一中台邮箱。

## 启动

复制 `.env.example` 为 `.env`，填写 SMTP 信息后运行：

```bash
npm run relay:local
```

## 建议

每台远程电脑使用一个独立发件邮箱和 SMTP 授权码。
