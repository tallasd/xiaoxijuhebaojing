# Windows 桌面通知采集器

第一阶段建议用“通知转发”方式接入千牛、微信、LINE、闲鱼、拼多多客户端等桌面软件。

可落地方式：

1. 能通过系统通知读取的 App，使用 Windows 通知监听器或 PowerShell 辅助脚本上报。
2. 不能稳定读取通知内容的 App，先上报“哪个客户端出现新提醒、窗口标题、时间、设备”。
3. 客服仍在原客户端回复，中台只负责提醒、派单和状态追踪。

`send-sample-notification.ps1` 是一个上报样例。真实采集时可把通知标题、正文、进程名替换进去。
